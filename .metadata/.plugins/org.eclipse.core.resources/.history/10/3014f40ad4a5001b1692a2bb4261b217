/** 
 * 
 */
package crs_sim.utils

import crs_sim.body.EnvObject
import java.util.Collection
import org.arakhne.afc.math.tree.node.QuadTreeNode

/** 
 * @author Thomas
 * 
 */
class DefaultTreeBuilder {
	
	// to force the class to be static
	private new {}
	
	def buildTree(tree : QuadTreeNode, collec : Collection<EnvObject>) : QuadTreeNode {
		
	}
	
	// mettre que du static
}
