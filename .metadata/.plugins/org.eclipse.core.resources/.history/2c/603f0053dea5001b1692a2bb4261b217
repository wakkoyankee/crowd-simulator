/** 
 * 
 */
package crs_sim.utils

import crs_sim.body.EnvObject
import java.util.Collection
import crs_sim.environment.DefaultQuadTreeNode

/** 
 * @author Thomas
 * 
 */
class DefaultTreeBuilder {
	
	// to force the class to be static
	private new {}
	
	def buildTree(tree : DefaultQuadTreeNode, collec : Collection<EnvObject>) : DefaultQuadTreeNode {
		for (element : collec){
			tree.insert(element)	
		}
		return tree;
	}
	
	// mettre que du static
}
